import 'package:flutter/material.dart';

class BBSettingsPage extends StatelessWidget {
  const BBSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
